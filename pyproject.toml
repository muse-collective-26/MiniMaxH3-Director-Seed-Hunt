[project]
name = "muse-minimax-director-seed-hunt"
description = "Timeline-based director node for MiniMax H3 — visual CUT/segment editor, multi-character reference images, reference video + reference audio (with per-clip audio pairing for lipsync/voice reference), First/Last Frame mode, automatic chunking with a hybrid hard-lock continuation option, and a Seed Hunt toggle that scouts 4 candidate seeds in one run for use with Muse Minimax Refine's second-pass upscale."
version = "1.1.0"
license = { file = "LICENSE" }

[project.urls]
Repository = "https://github.com/muse-collective-26/MiniMaxH3-Director-Seed-Hunt"

[tool.comfy]
PublisherId = "muse-collective"
DisplayName = "Muse Minimax Director (Seed Hunt)"
Icon = ""
